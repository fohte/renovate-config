{
  extends: ['github>fohte/renovate-config:base.json5'],

  packageRules: [
    {
      // Keep renovate unpinned to always get the latest version for E2E tests
      matchPackageNames: ['renovate'],
      rangeStrategy: 'replace',
    },
  ],
}
